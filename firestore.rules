rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true; // Allow anyone to read data
      allow write: if request.auth != null; // Allow authenticated users to write data
    }
  }
}